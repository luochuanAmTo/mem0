```py
from ollama import Client
import numpy as np

# 初始化客户端
client = Client(host="http://localhost:11434")

# 要测试的文本（模拟 mem0 会处理的内容）
texts = [
    "Alex is a vegetarian",
    "Alex is allergic to nuts"
]

print("=" * 50)
print("Embedding Results")
print("=" * 50)

for text in texts:
    response = client.embeddings(
        model="nomic-embed-text",
        prompt=text
    )

    embedding = response["embedding"]

    print(f"\nText: {text}")
    print(f"Vector length: {len(embedding)}")
    print(f"First 10 dims: {embedding[:10]}")
    print(f"Mean value: {np.mean(embedding):.6f}")
```

![image-20260428203952184](C:\Users\AmTo2\AppData\Roaming\Typora\typora-user-images\image-20260428203952184.png)
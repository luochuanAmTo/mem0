```py
from ollama import Client
import json

client = Client(host="http://localhost:11434")

# 模拟 mem0 的输入
messages = [
    {"role": "user", "content": "Hi, I'm Alex. I'm a vegetarian and allergic to nuts."},
    {"role": "assistant", "content": "Got it."}
]

# mem0 本质就是构造一个 prompt
prompt = f"""
You are a memory extraction system.
Extract important long-term information about the user from the conversation.
Only extract facts that are:
- Persistent (not temporary)
- Useful for personalization
Return ONLY valid JSON in this format:
[
  {{
    "type": "fact",
    "content": "..."
  }}
]

Conversation:
{messages}
"""

# 调用本地 llama3
response = client.chat(
    model="llama3",
    messages=[{"role": "user", "content": prompt}],
    options={
        "temperature": 0.1
    }
)

output = response["message"]["content"]

print("=== RAW MODEL OUTPUT ===")
print(output)
```

![image-20260428203713579](F:\mem0\本地ollama功能测试_brief\assets\image-20260428203713579.png)